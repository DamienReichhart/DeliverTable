@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // On redirige vers /login en gardant l'URL actuelle en mémoire si besoin (optionnel)
        Navigation.NavigateTo("");
    }
}